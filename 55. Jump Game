class Solution {
    public boolean canJump(int[] nums) {
        int t=0;
        for(int i=0;i<=t;i++){
            t=Math.max(t,i+nums[i]);
            if(t>=nums.length-1){
                return true;
            }
        }
        return false;
    }
}
